package auth

import (
	"github.com/gofiber/fiber/v2"
)

func SetRoutes(app *fiber.App) {
	// var authHandler auth_handlers.AuthHandler

	// authApi := app.Group("auth")
	// authApi.Get("otp", authHandler.GetOtp)
	// authApi.Post("login", authHandler.Login)
	// authApi.Post("register", authHandler.Register)
	// authApi.Post("forgot", authHandler.ForgotPassword)
	// authApi.Post("change", session.TokenMiddleware, authHandler.ChangePassword)
}
